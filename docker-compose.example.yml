services:
  firewalla-abuse-guard:
    build: .
    restart: unless-stopped
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    volumes:
      - ./config.yml:/app/config.yml:ro
      - guard-state:/app/data

volumes:
  guard-state:
